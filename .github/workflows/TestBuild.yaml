name: Test images

on:
  pull_request:
    branches:
      - master
jobs:
  matrix:
    name: Test build
    strategy:
      matrix:
        tags:
          - alpine-base
          - alpine-base
          - alpine-base-s6
          - alpine-base-s6
          - debian-base
          - debian-base-s6
          - dotnet-base
          - dotnet-base-s6
          - elastic-ek
          - frontend
          - go
          - go-base
          - hacs-action
          - integration
          - monster
          - netdaemon
          - nodejs-base
          - nodejs-base-s6
          - python
          - python-base
          - python-base-s6
          - v
          - v-base

    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@master

      - name: Set up Docker Buildx
        id: buildx
        uses: crazy-max/ghaction-docker-buildx@v1
        with:
          buildx-version: latest

      - name: Install requirements
        run: make init

      - name: Test building ${{tags}}
        run: python3 ./Build.py ${{tags}}

